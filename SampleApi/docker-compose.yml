version: '3.1'

services:
  elasticsearch:
    container_name: els
    image: docker.elastic.co/elasticsearch/elasticsearch:8.7.1
    ports:
      - 9200:9200
    volumes:
      - elasticsearch-data:/usr/share/elasticsearch/data
    environment:
      - xpack.security.enabled=false
      - discovery.type=single-node
    networks:
      - elastic

  kibana:
    container_name: kibana
    image: docker.elastic.co/kibana/kibana:8.7.1
    ports:
      - 5601:5601
    depends_on:
      - elasticsearch
    environment:
      - ELASRICSEARCH_URL=http://localhost:9200
    networks:
      - elastic

  api:
    container_name: sample-api-instance
    build:
      context: .
      dockerfile: dev-dockerfile
    image: sample-api
    working_dir: /tmp/sample-api
    volumes:
      - .:/tmp/sample-api
    tty: true
    depends_on:
      - kibana
    ports:
      - 5176:5176
    networks:
      - elastic

networks:
  elastic:
    driver: bridge

volumes:
  elasticsearch-data:
  .: